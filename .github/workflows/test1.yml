name: Update Readme File
on:
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

# 確保 cron 任務不會重疊運行
concurrency: 
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  update-readme:
    name: Update Readme Stats and Feed
    runs-on: ubuntu-latest
    steps:
      # 1. 簽出代碼
      - name: Checkout main branch
        uses: actions/checkout@v4
        with:
          ref: main 

      # 2. 運行 Last.fm 腳本
      - name: Update Last.fm README
        uses: dxnter/lastfm-readme@v1.6.0
        with:
          LASTFM_API_KEY: ${{ secrets.LASTFM_API_KEY }}
          LASTFM_USER: GhostMxv
          SHOW_TITLE: false
          LOCALE: en-US

      # 3. 運行 sed 清理
      - name: Remove blockquote from README
        run: |
          sed -i 's/> 🎶/-/g' README.md
          sed -i 's/> ∙/-/g' README.md

      # 4. 運行 Blog Feed 腳本
      - name: Update Blog Feed
        uses: sarisia/actions-readme-feed@v1
        id: feed
        with:
          file: 'README.md'
          max_entry: 6
          format: '- ${monthshort} ${day} ${year} - [${title}](${url})'
          timezone: 'Asia/Hong_Kong'
          url: |
            https://gholts.top/rss.xml

      # 5. 【關鍵修復】
      # 在提交前，拉取並 rebase 遠端更改
      # git pull --rebase 會自動儲存 (stash) 您未提交的本地更改 (README.md),
      # 拉取遠端最新提交, 然後再應用 (unstash) 您的本地更改。
      - name: Pull and Rebase
        run: git pull --rebase

      # 6. 提交所有更改
      # 此時, 本地分支已包含遠端最新提交，可以安全推送
      - name: Commit all changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs(readme): update stats and blog feed"
          commit_user_name: "README Bot"
          commit_user_email: "bot@example.com"
