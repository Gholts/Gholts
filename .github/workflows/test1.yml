name: Update Readme File
on:
  schedule:
    - cron: '0 */5 * * *'
  workflow_dispatch:

# 確保 cron 任務不會重疊運行
concurrency: 
  group: ${{ github.workflow }}
  cancel-in-progress: false

jobs:
  update-readme:
    name: Update Readme Stats and Feed
    runs-on: ubuntu-latest
    steps:
      # 1. 簽出代碼
      - name: Checkout main branch
        uses: actions/checkout@v4
        with:
          ref: main 

      # 2. 運行 Last.fm 腳本
      - name: Update Last.fm README
        uses: dxnter/lastfm-readme@v1.6.0
        with:
          LASTFM_API_KEY: ${{ secrets.LASTFM_API_KEY }}
          LASTFM_USER: GhostMxv
          SHOW_TITLE: false
          LOCALE: en-US

      # 3. 運行 sed 清理
      - name: Remove blockquote from README
        run: |
          sed -i 's/> 🎶/-/g' README.md
          sed -i 's/> ∙/-/g' README.md

      # 4. 運行 Blog Feed 腳本
      - name: Update Blog Feed
        uses: sarisia/actions-readme-feed@v1
        id: feed
        with:
          file: 'README.md'
          max_entry: 6
          format: '- ${monthshort} ${day} ${year} - [${title}](${url})'
          timezone: 'Asia/Hong_Kong'
          url: |
            https://gholts.top/rss.xml

      # 5. 【關鍵修復】
      # 在 stash 和 pull 之前，必須先配置 Git 用戶
      - name: Configure Git
        run: |
          git config --global user.name "README Bot"
          git config --global user.email "bot@example.com"

      # 6. 【關鍵修復】
      # 儲存本地更改，拉取遠端，然後重新應用更改
      - name: Stash local changes, pull remote, and apply
        run: |
          git stash
          git pull --rebase
          git stash pop || true # "|| true" 可防止在沒有 stash 時出錯

      # 7. 提交所有更改
      # action 會使用自己的 user/email 覆蓋 global 設置
      - name: Commit all changes
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "docs(readme): update stats and blog feed"
          commit_user_name: "README Bot"
          commit_user_email: "bot@example.com"
